# Routes /sightings to the local sighting-store service (see
# flightradar-sighting-store.service), keeping the browser's fetch same-origin.
server.modules += ( "mod_proxy" )

$HTTP["url"] =~ "^/sightings" {
  proxy.server = ( "" => ( ( "host" => "127.0.0.1", "port" => 8083 ) ) )
}
