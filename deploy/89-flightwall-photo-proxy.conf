# Routes /photo/<hex> to the local photo-proxy service (see
# flightwall-photo-proxy.service), keeping the browser's fetch same-origin.
server.modules += ( "mod_proxy" )

$HTTP["url"] =~ "^/photo/" {
  proxy.server = ( "" => ( ( "host" => "127.0.0.1", "port" => 8081 ) ) )
}
