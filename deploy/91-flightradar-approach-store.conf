# Routes /approaches to the local approach-store service (see
# flightradar-approach-store.service), keeping the browser's fetch same-origin.
server.modules += ( "mod_proxy" )

$HTTP["url"] =~ "^/approaches" {
  proxy.server = ( "" => ( ( "host" => "127.0.0.1", "port" => 8082 ) ) )
}
