# Routes /wake to the local display-wake service (see
# flightradar-wake.service), keeping the browser's fetch same-origin.
server.modules += ( "mod_proxy" )

$HTTP["url"] =~ "^/wake" {
  proxy.server = ( "" => ( ( "host" => "127.0.0.1", "port" => 8084 ) ) )
}
